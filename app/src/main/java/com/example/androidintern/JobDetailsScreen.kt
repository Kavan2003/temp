package com.example.androidintern

import androidx.compose.runtime.Composable

@Composable
fun JobDetailsScreen(job: Job, jobsViewModel: JobsViewModel) {
    // Display job details
    // On bookmark click, call jobsViewModel.bookmarkJob(job)
}